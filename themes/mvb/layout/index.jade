extends base

block content
  h1 Heading
  include partial/recent-posts

  //- each index_page in theme.index_pages
    //- each pg in site.pages.toArray()
      //- if pg.title.toLowerCase() === index_page
        //- h1= pg.title
        //- div!= pg.content

  include partial/pagination
